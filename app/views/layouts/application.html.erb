<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : 'Regrapher showcase app (rails 4.2)' %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : 'Regrapher showcase app (rails 4.2)' %>">
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main" class="container">
        <div class="alert alert-info <%= 'hidden' if request.path == about_page_path %>">
          This application demonstrates live monitoring with
          <b><%= link_to 'Regrapher', 'https://www.regrapher.com', target: '_blank'%></b>. More information in the
          <b><%= link_to 'about page', about_page_path%></b>.
        </div>
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>
  </body>
</html>
